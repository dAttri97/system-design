package com.attri.systemdesign.lld.venue_booking.model;

/** Sports a court can be configured for. Adding a new sport is a one-line change. */
public enum Sport {
    BADMINTON,
    TENNIS,
    SQUASH,
    TABLE_TENNIS,
    FOOTBALL,
    CRICKET,
    BASKETBALL
}
